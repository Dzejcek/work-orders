import { AssignedToNamesPipe } from './assigned-to-names.pipe';

describe('AssignedToNamesPipe', () => {
  it('create an instance', () => {
    const pipe = new AssignedToNamesPipe();
    expect(pipe).toBeTruthy();
  });
});
